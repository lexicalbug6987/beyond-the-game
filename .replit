modules = ["nodejs-20", "web", "bash"]
[agent]
expertMode = true
stack = "BEST_EFFORT_FALLBACK"

[nix]
channel = "stable-25_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "bash scripts/replit-dev.sh"
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 8787
externalPort = 3000

[deployment]
deploymentTarget = "vm"
run = ["bash", "-c", "node apps/server/dist/index.js & npx vite preview --port 5000 --host 0.0.0.0 apps/web"]
build = ["bash", "-c", "npm run build -w @team-culture-sim/sim-engine && npm run build -w @team-culture-sim/server && npm run build -w @team-culture-sim/web"]
